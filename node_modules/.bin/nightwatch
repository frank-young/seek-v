#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.npminstall/nightwatch/0.9.9/nightwatch/bin/nightwatch" "$@"
  ret=$?
else 
  node  "$basedir/../.npminstall/nightwatch/0.9.9/nightwatch/bin/nightwatch" "$@"
  ret=$?
fi
exit $ret
