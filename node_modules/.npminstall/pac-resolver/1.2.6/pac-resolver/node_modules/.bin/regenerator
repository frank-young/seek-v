#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../../regenerator/0.8.46/regenerator/bin/regenerator" "$@"
  ret=$?
else 
  node  "$basedir/../../../../../regenerator/0.8.46/regenerator/bin/regenerator" "$@"
  ret=$?
fi
exit $ret
